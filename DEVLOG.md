# RS-Ground 개발 일지

이 문서는 작업이 하나 끝날 때마다 "무엇을 만들었는지 / 왜 그렇게 만들었는지"를 기록하는 곳입니다.
개발 지식이 없어도 나중에 다시 읽었을 때 이해할 수 있도록, 개념 설명부터 적어둡니다.

---

## 기술 스택 (사용하는 도구들)

| 이름 | 한 줄 개념 | 이 프로젝트에서 쓰는 이유 |
|---|---|---|
| **React** | 화면을 "컴포넌트"라는 작은 조각(버튼, 카드, 페이지 등)으로 나눠서 만들고, 데이터가 바뀌면 화면을 알아서 다시 그려주는 라이브러리 | 화면 하나하나를 재사용 가능한 조각으로 나눠 관리하기 위해 |
| **Vite** | 코드를 브라우저가 이해할 수 있는 형태로 변환하고, 저장할 때마다 브라우저에 바로 반영해주는 개발 서버/빌드 도구 | `npm run dev`로 개발 중 실시간 미리보기, `npm run build`로 배포용 파일 생성 |
| **react-router-dom** | 주소창의 경로(`/`, `/login`, `/knowledge` 등)에 따라 다른 화면을 보여주는 "라우팅" 기능을 제공하는 라이브러리 | 화면을 하나씩 계속 추가해 나갈 계획이라, 처음부터 "주소 ↔ 화면" 규칙을 잡아두면 나중에 화면을 붙이기 쉬움 |
| **lucide-react** | 아이콘 이미지를 코드로 바로 쓸 수 있게 해주는 아이콘 모음 라이브러리 | 사이드바·버튼 등에 쓰는 아이콘들 |

> 참고: 아직 서버(백엔드)는 없습니다. 로그인 버튼을 누르면 `/api/auth/login`으로 요청을 보내지만, 받아줄 서버가 없어서 지금은 실패(404)합니다. 이는 "프론트엔드만 먼저 만들자"는 이번 단계의 목표에 맞는 정상적인 상태입니다.

---

## 프로젝트 구조 (폴더가 하는 역할)

```
src/
  main.jsx              화면을 처음 그리는 시작점. 여기서 라우터(BrowserRouter)와 로그인 상태(AuthProvider)를 전체 앱에 씌운다.
  App.jsx               "이 주소면 이 화면"이라는 라우팅 규칙표
  context/               여러 화면이 공통으로 알아야 하는 상태(예: 로그인 여부)를 저장하는 곳
  layouts/               사이드바+상단바처럼 여러 페이지가 공통으로 쓰는 "틀"
  pages/                 실제 화면 하나하나 (로그인 페이지, 대시보드 페이지 등)
  components/            여러 페이지에서 재사용하는 작은 부품 (사이드바, 상단바, 토스트 알림 등)
  styles/                여러 페이지가 공통으로 쓰는 디자인(색상, 버튼 모양 등)
  data.js / utils.js / api.js   목업 데이터, 계산 함수, 서버 요청 함수
```

**왜 이렇게 나눴나요?**
페이지가 하나만 있을 때는 파일 하나에 다 몰아 넣어도 괜찮지만, "화면을 하나씩 계속 붙여나갈 것"이라고 하셔서, 새 화면을 추가할 때 기존 코드를 거의 안 건드리고 폴더만 하나 늘리면 되도록 미리 구조를 잡았습니다. 새 화면 하나 = `pages/새화면/` 폴더 하나 + `App.jsx`에 라우트 한 줄 추가, 라는 규칙입니다.

---

## 작업 기록

### 2026-07-05 — 로그인 화면 + 메인 대시보드 (프론트엔드 뼈대)

**무엇을 만들었나:**
- 랜딩 화면 + 로그인 폼 (`pages/login/LoginPage.jsx`) — 기존 디자인(다크 테마, "Your data runs the world" 히어로)을 그대로 재사용
- 메인 대시보드(홈) 화면 (`pages/dashboard/DashboardPage.jsx`) — 캘린더, 할 일 목록, 빠른 메모
- 공통 틀 (`layouts/AppLayout.jsx`) — 사이드바 + 상단바 + 검색(⌘K) 팔레트를 모든 페이지에 공통으로 씌움
- 아직 만들지 않은 화면들(지식, AI 채팅, 보고서, 에이전트, 일정, 할 일, 메모, 설정)은 클릭하면 "화면 준비 중" 이라는 임시 화면(`pages/coming-soon/ComingSoonPage.jsx`)이 뜨도록 라우트만 미리 뚫어둠

**왜 이렇게 결정했나 (선택한 옵션):**
- 라우팅: `react-router-dom` 도입 → 화면마다 고유한 주소를 가지므로 새로고침해도 어떤 화면인지 유지되고, 새 화면 추가가 "라우트 한 줄"로 끝남
- 스타일: 컴포넌트별 CSS 파일 분리 → 예전처럼 CSS 파일 하나(500줄+)에 다 몰아넣지 않고, 페이지/컴포넌트 옆에 같은 이름의 `.css` 파일을 둠
- 디자인: 기존 로그인/대시보드 디자인 그대로 유지 (새로 디자인하지 않음)

**검증한 내용:**
- `npm run build` 정상 통과
- headless 브라우저(Playwright)로 실제로 접속해서: 랜딩 화면 → 로그인 폼 → (백엔드 없어서 로그인 실패 메시지 정상 표출) → 대시보드 화면 → 사이드바에서 "지식" 클릭 시 준비중 화면까지 스크린샷으로 확인, 콘솔 에러 없음

**아직 안 된 것 / 다음에 할 수 있는 것:**
- 백엔드(로그인 서버, 데이터베이스)는 아직 전혀 없음
- 지식 / AI 채팅 / 보고서 / 에이전트 / 일정 / 할 일 / 메모 / 설정 — 전부 "준비 중" 상태. 하나씩 골라서 이어서 만들면 됨

---

### 2026-07-05 (이어서) — 프로덕션 대비 보완 (가드/404/오류화면/lazy/접근성/테스트)

지난 작업 리뷰(코드 검토)에서 "빠졌다"고 지적된 항목들을 하나씩 점검하고, 실제로 지금 단계에 맞는 것만 적용했습니다.

**새 개념 설명:**
- **Not Found(404) 페이지**: 없는 주소로 들어왔을 때 조용히 다른 곳으로 보내는 대신, "이 주소는 없습니다"라고 알려주는 화면. 사용자가 왜 화면이 바뀌었는지 헷갈리지 않게 해줍니다.
- **Error Boundary(오류 경계)**: 화면을 그리다가 코드에서 예외가 터지면 앱 전체가 하얀 화면으로 멈추는 대신, "문제가 발생했습니다 + 새로고침" 화면을 보여주는 안전망. React에서는 클래스 컴포넌트로만 만들 수 있는 특수한 기능입니다.
- **Lazy loading(지연 로딩)**: 원래는 모든 페이지 코드를 한 번에 다운로드했는데, 이제는 방문하는 페이지의 코드만 그때그때 받아옵니다. 페이지가 많아질수록 처음 접속 속도가 느려지지 않게 해줍니다. (`React.lazy` + `<Suspense>`)
- **접근성(aria-label)**: 아이콘만 있고 글자가 없는 버튼은 스크린리더 사용자에게 "이름 없는 버튼"으로 들립니다. `aria-label`로 "이 버튼은 로그아웃입니다" 같은 숨은 이름표를 붙여줍니다.
- **테스트(vitest)**: 코드를 수정할 때마다 사람이 일일이 클릭해보지 않아도, "로그인 안 하면 로그인 화면으로 가는가?" 같은 규칙을 자동으로 확인해주는 도구.

**무엇을 했나:**
- `components/protected-route/ProtectedRoute.jsx` — 기존 `App.jsx` 안에 있던 로그인 가드를 별도 파일로 분리 (원래도 있었지만 재사용하기 좋게 위치만 정리)
- `pages/not-found/NotFoundPage.jsx` — 알 수 없는 주소(`*` 라우트)로 접근 시 표시
- `components/error-boundary/ErrorBoundary.jsx` — `main.jsx`에서 앱 전체를 감싸는 안전망으로 추가
- `App.jsx` — 페이지 컴포넌트들을 `React.lazy`로 불러오도록 변경, `<Suspense>`로 감쌈
- 아이콘 전용 버튼들(로그아웃, 검색 열기, 할 일 체크, 상단 검색/빠른추가 등)에 `aria-label` 추가, 클릭 동작이 없던 달력 날짜 칸은 `<button>`에서 `<div>`로 변경(가짜 버튼 제거)
- `vitest` + `@testing-library/react` 설치, `npm test` 스크립트 추가, 테스트 3개 작성:
  - 로그인 안 하면 `/`에서 로그인 화면으로 이동하는지
  - 없는 주소는 404 화면을 보여주는지
  - Login 버튼을 누르면 아이디/비밀번호 폼이 나타나는지
- 모바일(375px) 화면에서 대시보드 카드가 화면 높이에 잘려서 안 보이던 버그 발견 → 좁은 화면에서는 페이지 자체가 스크롤되도록 CSS 수정 (`DashboardPage.css` 모바일 미디어쿼리)

**검증한 내용:**
- `npm run build` 통과 — 페이지별로 별도 JS/CSS 파일(chunk)이 생성되는 것을 확인해 lazy loading이 실제로 적용됐음을 확인
- `npm test` 통과 (3개 테스트)
- headless 브라우저로 모바일(375px)·태블릿(768px) 화면 스크린샷 확인, 새로고침해도 로그인(모의) 상태와 현재 페이지가 유지되는지 확인, Ctrl+K로 커맨드 팔레트가 열리고 Esc로 닫히는지 확인 — 콘솔 에러 없음

**아직 안 된 것:**
- 진짜 백엔드가 없어서 "새로고침 후 로그인 유지"는 서버 응답을 흉내낸(mock) 상태로만 검증함. 실제 서버가 붙으면 다시 한 번 실제 환경에서 확인 필요
- 스크린리더로 직접 들어보는 수동 점검(자동 검사로는 aria-label 존재 여부만 확인 가능)까지는 하지 않음

---

### 2026-07-05 (이어서 2) — 개발용 가짜 로그인 서버 추가

**무엇을, 왜:**
진짜 백엔드가 없으면 로그인 버튼을 눌러도 항상 실패해서 대시보드 화면을 직접 볼 방법이 없었습니다. 그래서 `vite.config.js`에 **개발 서버 전용** 가짜 인증 API(`mockAuth` 플러그인)를 추가했습니다.

- `POST /api/auth/login` — 아이디/비밀번호를 실제로 검사하지 않고 무조건 성공 처리, 로그인 상태를 나타내는 쿠키 발급
- `GET /api/auth/me` — 그 쿠키가 있으면 "로그인된 상태"로 응답 (새로고침해도 로그인 유지되는 이유)
- `POST /api/auth/logout` — 쿠키 삭제

**중요 — 이건 진짜 보안이 아닙니다:** 비밀번호를 확인하지 않으므로 아무 값이나 입력해도 로그인됩니다. **오직 로컬 개발(`npm run dev`)에서 화면을 확인하기 위한 임시 장치**이고, `vite build`로 만드는 배포용 결과물에는 포함되지 않습니다(이 코드는 dev 서버에서만 실행됨).

**다음에 진짜 백엔드를 만들 때 할 일:** `vite.config.js`에서 `mockAuth()` 플러그인 부분을 통째로 지우고, `server.proxy`에 실제 백엔드 주소를 연결하면 됩니다.

---

### 2026-07-07 — RAG 파이프라인 설계 검토 + PRD 작성 (코드 없음, 문서 작업)

**무엇을 했나:**
다음 기능으로 **RAG**(내 문서에서 관련 내용을 먼저 찾아 근거 있는 AI 답변을 만드는 기법)를 만들기로 하고, 구현 전에 스택을 검증한 뒤 요구사항 문서 **[docs/PRD-RAG.md](docs/PRD-RAG.md)** 를 작성했습니다. 코드는 아직 한 줄도 안 짰습니다 — "무엇을 왜 만들지"를 먼저 문서로 확정한 단계입니다.

**선택한 스택과 각각의 역할 (개념):**
- **업스테이지 임베딩 API** — 텍스트를 "의미가 비슷하면 가까운 좌표"가 되는 숫자 벡터(4096차원)로 변환. 한국어 성능이 좋아서 선택
- **ChromaDB** — 그 벡터들을 저장해두고 "이 벡터와 가장 가까운 것 N개"를 빠르게 찾아주는 벡터 데이터베이스. 서버 설치 없이 파일로 동작해 개인 프로젝트에 적합
- **UMAP + Plotly** — 4096차원은 사람이 볼 수 없으니 UMAP으로 2차원으로 눌러서, Plotly로 마우스오버 가능한 산점도(HTML)로 그림. 임베딩이 잘 됐는지 눈으로 진단하는 용도

**검토에서 나온 핵심 결정 (놓치면 나중에 재작업이 필요한 것들):**
1. **문서는 `embedding-passage`, 질문은 `embedding-query` 모델** — 업스테이지는 저장용/검색용 모델이 나뉘어 있어서 섞으면 검색 품질이 크게 떨어짐
2. **ChromaDB 컬렉션을 만들 때 거리 방식을 cosine으로 지정** — 기본값(L2)인 채로 만들면 나중에 바꿀 수 없어 전부 다시 임베딩해야 함
3. **임베딩 캐시** — "같은 텍스트를 같은 모델로 두 번 임베딩하지 않는다"는 원칙으로, 텍스트 해시를 키로 로컬(SQLite)에 저장 → 재실행·재실험 시 API 비용 0원
4. **결정적 ID + upsert** — 저장 ID를 `파일명::청크번호`로 만들어, 파이프라인을 여러 번 돌려도 데이터가 중복으로 불어나지 않게 함
5. **단계마다 중간 산출물을 파일로 남김** (청크 목록, UMAP 좌표 등) — 어느 단계가 문제인지 분리 진단 + 뒷단계만 재실행 가능

**계획에서 원래 빠져 있다가 추가된 것:** 청킹(문서를 검색 단위로 자르는) 전략. 임베딩 모델보다 검색 품질에 더 큰 영향을 주는 부분이라 PRD에 시작값(500~800자, 오버랩 10~15%)을 명시했습니다.

**아직 안 된 것 / 다음 단계:**
- 구현 전체 (backend/rag/ 파이프라인) — PRD 승인 후 착수
- 구현 시작 시 사용자와 결정할 것: 초기 투입 문서를 뭘로 할지, Python 실행 환경(venv/버전)
- 업스테이지 API 키 발급 필요 (`.env`로 관리, 절대 커밋 금지)

---

### 2026-07-07 (이어서) — RAG 파이프라인 코드 구현 (1단계: 임베딩 API 호출 전까지 검증 완료)

**무엇을 만들었나 (`backend/rag/`):**
- `config.py` — 경로·모델명(embedding-passage/query)·청킹 크기 등 설정을 한곳에 모음
- `chunker.py` — 문서를 문단 우선으로 자르고(너무 길면 문장 단위로 더 쪼갬), 청크마다 `source`/`char_start`/`content_hash` 등 메타데이터를 붙여 `chunks.jsonl`로 저장
- `embedder.py` — 업스테이지 API 호출 + **SQLite 임베딩 캐시**(`sha256(모델명+텍스트)` 키로 저장, 캐시에 있으면 API를 아예 호출하지 않음). `embed_passages()`/`embed_query()`로 문서용/질문용 모델을 코드 레벨에서 분리해 섞어 쓰는 실수를 막음
- `store.py` — ChromaDB 저장/검색. 컬렉션 생성 시 `hnsw:space: cosine` 지정, 문서 ID를 `{파일명}::{청크번호}`로 고정해 `upsert`(재실행해도 데이터 안 불어남)
- `visualize.py` — UMAP(고정 시드, cosine 거리) → Plotly 인터랙티브 산점도 HTML. 좌표는 `umap_coords.json`으로 캐시
- `pipeline.py` — `python -m rag.pipeline index` (전체 실행), `python -m rag.pipeline search "질문"` (검색)만 실행하면 되는 CLI 진입점

**미결정 두 가지를 정해서 진행:**
- Python 실행 환경 → `backend/venv`, Python 3.11.9(umap-learn/numba 호환성이 안정적인 버전) 사용, `backend/requirements.txt`에 전 패키지 버전 고정
- 초기 테스트 문서 → 실제 문서가 아직 없어서, 이미 있는 프로젝트 자체 문서(AGENT.md, DEVLOG.md, PRD-RAG.md)를 `data/raw/`에 복사해 파이프라인 동작 확인용으로 사용

**검증한 내용 (API 키 없이 가능한 범위):**
- 청킹: 문서 3개 → 청크 49개 생성 확인, JSONL로 저장 후 다시 읽어도 개수 일치(왕복 검증)
- ChromaDB: 컬렉션 생성 및 cosine 거리 설정 확인
- `.env`(API 키), `data/`, `output/`, `backend/venv`를 `.gitignore`에 추가 — 커밋 안 되는 것 확인

**아직 검증 못 한 것 (업스테이지 API 키 필요):**
- 실제 임베딩 호출 → ChromaDB 저장 → 검색 → UMAP/Plotly 시각화까지의 전체 흐름
- 두 번 실행해도 데이터가 안 불어나고 캐시 덕분에 API가 재호출되지 않는지(PRD 성공 기준 2번)
- API 키를 받는 대로 `python -m rag.pipeline index` 한 번 실행해 전체 파이프라인을 끝까지 확인할 예정

---

### 2026-07-07 (이어서 2) — 문서 분류(대분류/소분류) 메타데이터 추가

**질문의 배경:** "임베딩할 문서를 대분류/소분류로 미리 나눠야 하나?"라는 질문에서 시작. 정리해보니 임베딩 문서 대상이 두 성격으로 나뉨 — ① 예산/회계처럼 완전히 다른 종류, ② 기술별(AI/클라우드) → 사업별(AI업무비서/AI챗봇)처럼 서로 비슷한 종류.

**개념 설명 — 왜 미리 분류를 안 해도 되는가:**
폴더 정리는 "그 폴더를 뒤져서" 찾기 위한 방식이지만, 벡터 검색(임베딩)은 "의미가 비슷한 것"을 자동으로 찾아줍니다. 그래서 ①처럼 완전히 다른 문서는 벡터 공간에서도 서로 멀리 떨어져 있어 **검색할 때 저절로 안 걸립니다** — 미리 분류할 필요가 없다는 뜻. 반대로 ②처럼 서로 비슷한 문서는 벡터 공간에서도 가까이 있어서 검색 결과가 섞일 수 있는데, 이때는 "그중에서도 특정 사업만" 좁혀서 찾을 수 있는 **필터**가 있으면 좋습니다.

**어떻게 만들었나 (분류를 손으로 태깅하지 않고, 폴더 정리로 공짜로 얻는 방식):**
- `chunker.py`: `data/raw/` 밑의 폴더 경로에서 상위 3단계(예: `기술/AI/AI챗봇/기획서.md` → 대분류=기술, 중분류=AI, 소분류=AI챗봇)를 자동으로 뽑아 `category_l1`/`category_l2`/`category_l3` 메타데이터로 저장. 폴더를 3단보다 얕게 두면 남는 값은 빈 문자열
- `store.py`: ChromaDB 검색 시 이 분류값으로 좁혀서 찾을 수 있는 `where` 조건 빌더 추가
- `pipeline.py`: `python -m rag.pipeline search "질문" --l1 기술 --l2 AI --l3 AI챗봇`처럼 CLI에서 분류를 좁혀 검색 가능

**검증한 내용:**
- 임시 폴더(`data/raw/기술/AI/AI챗봇/`, `data/raw/예산회계/`)로 청킹 테스트 → 대/중/소분류가 의도대로 뽑히는지 확인 후 임시 파일 삭제
- 윈도우 경로 구분자(`\`) 문제 발견 → `source`/폴더 경로를 슬래시(`/`)로 정규화해서 수정 (다른 OS에서도 동일하게 동작하도록)
- ChromaDB where 절 빌더 단위 테스트(조건 0개/1개/2개 각각 확인)

**결론:** 문서를 넣을 때 분류 체계를 미리 설계할 필요 없이, **그냥 폴더로 자연스럽게 정리해서 넣기만 하면** 나중에 필요할 때 분류 필터링을 쓸 수 있는 구조가 됐습니다.

---

### 2026-07-07 (이어서 3) — 코드 리뷰로 발견된 버그 2건 수정 (청킹 상한 미보장 / 재색인 시 오래된 청크 잔존)

사용자가 코드를 직접 검토하다가 두 가지 문제를 지적, 검증 후 둘 다 실제 버그로 확인되어 수정했습니다.

**버그 ① 청킹이 chunk_size를 항상 보장하지 않음:**
- **원인:** `_split_long_paragraph`가 마침표(`.!?。`) 뒤 공백 기준으로만 문단을 자르는데, 표·URL·코드블록·디렉토리 트리처럼 마침표가 없는 문단은 "문장 1개"로 취급돼서 아예 안 잘림
- **실제로 확인된 영향:** 지금 있는 테스트 문서 3개만으로도 청크 49개 중 11개(22%)가 600자 제한을 넘었고, 가장 심한 건 1246자(코드 구조 트리 부분)로 목표치의 2배 이상. 업스테이지 토큰 제한(~4,000토큰)까지는 여유 있지만, 실제 업무 문서(표·긴 URL 포함)가 들어오면 API 호출이 실패할 위험
- **수정:** `_split_long_paragraph`에 마지막 안전장치로, 문장 분할 후에도 여전히 `max_size`를 넘는 조각을 고정 길이로 강제 슬라이싱하는 하드 스플릿 추가
- **검증:** 마침표 없는 2000자 텍스트로 단위 테스트(정확히 600자씩 4조각으로 분할 확인) + 실제 문서 3개 재청킹해서 최대 청크 길이가 1246자 → 682자(chunk_size 600 + 오버랩 80의 예상 범위 내)로 줄어든 것 확인

**버그 ② 재색인 시 삭제/축소된 문서의 오래된 청크가 ChromaDB에 남음:**
- **원인:** `upsert_chunks`가 이번 실행에서 나온 ID만 upsert하고, 이전엔 있었지만 이번엔 없는 ID는 그대로 둠 → 문서를 편집해서 청크 수가 줄거나 문서를 통째로 삭제해도 옛날 벡터가 검색 결과에 계속 나옴
- **수정:** `store.py`에 `reset_collection()` 추가 — 재색인(`run_index`) 시작 시 컬렉션을 통째로 지우고 다시 만든 뒤 이번 실행분만 채움. source별로 부분 삭제하는 방식도 검토했지만, "문서가 통째로 삭제된 경우"는 그 방식으로는 못 잡아서(사라진 파일은 이번 실행에 아예 안 나타나므로) 더 복잡해지기만 함. 임베딩 API는 이미 SQLite 캐시가 막아주고 있어서, 매번 컬렉션을 통째로 비우고 다시 채워도 **API 비용은 거의 늘지 않음**(ChromaDB 재삽입은 로컬 연산이라 값싼 작업)
- **검증:** 청크 3개짜리 문서가 1개로 줄어드는 상황을 시뮬레이션 → 재색인 후 컬렉션에 정확히 1개만 남고 오래된 2개가 사라지는 것 확인

---

### 2026-07-07 (이어서 4) — RAG 파이프라인 전체 흐름을 실제 업스테이지 API로 끝까지 검증

사용자가 업스테이지 API 키를 `backend/.env`에 넣어줘서, 그동안 API 키가 없어 못 해봤던 **실제 임베딩 호출 → ChromaDB 저장 → 검색 → 시각화**까지의 전체 흐름을 처음으로 끝까지 돌려봤습니다.

**막혔던 것 — 라이브러리 버전 충돌:**
UMAP 단계에서 `TypeError: check_array() got an unexpected keyword argument 'force_all_finite'` 오류 발생. 원인은 `umap-learn 0.5.7`이 내부적으로 쓰는 scikit-learn 함수 인자 이름이, pip가 자동으로 설치한 최신 scikit-learn(1.9.0)에서 이미 바뀌어 버렸기 때문. PRD 작성 때 "umap-learn과 numpy/scikit-learn 같은 의존성 버전이 자주 부딪힌다"고 미리 우려했던 게 실제로 발생한 사례입니다. `requirements.txt`에 `scikit-learn==1.5.2`(umap-learn 0.5.7과 호환되는 버전)를 명시적으로 고정해서 해결했습니다.

**검증한 내용 (PRD 성공 기준 충족 확인):**
- 문서 3개(agent.md, devlog.md, prd-rag.md) → 청크 51개 → 4096차원 벡터 51개 임베딩 성공
- 검색 테스트: `"청킹 전략은 어떻게 정했어"`로 검색했더니 실제로 PRD의 청킹 섹션이 1위로 나옴 — 의미 기반 검색이 실제로 동작함을 확인
- **캐시 검증**: 파이프라인을 두 번째 실행했을 때 51개 텍스트 전부 캐시에서 히트해서 **0.07초 만에 임베딩 완료**(API 재호출 0번) — "같은 텍스트를 두 번 임베딩하지 않는다"는 설계 원칙이 실제로 지켜짐
- **중복 방지 검증**: 두 번째 실행 후에도 ChromaDB 컬렉션이 51개로 그대로 유지(중복 저장 없음) — `reset_collection()`이 정상 동작
- `output/embedding_map.html` 생성 확인, 브라우저로 열어 실제 산점도 렌더링 확인

**결론:** PRD-RAG.md에 적어둔 성공 기준 5개 중 API 키가 있어야 확인 가능했던 항목들(실제 임베딩/검색/시각화, 재실행 시 캐시로 API 재호출 없음)까지 전부 통과했습니다. RAG 파이프라인 1단계(독립 Python 파이프라인)가 완성됐고, 다음은 PRD 6장에 적어둔 대로 서버화(FastAPI)·화면 연동이 후보입니다.

---

### 2026-07-07 (이어서 5) — 지식 탭: 문서 업로드→임베딩 화면 + 색상 시스템

**요청 배경:** 브랜드 컬러 5종(보라/초록/오렌지/노랑/파랑)에 역할을 부여한 가이드를 받았고, "지식 탭에 내 PC 문서를 업로드해서 임베딩시킬 수 있는 화면"을 만들어달라는 요청.

**새 개념 설명 — 왜 서버가 새로 필요했나:**
지금까지 만든 RAG 파이프라인(`backend/rag/`)은 **터미널에서 직접 실행하는 스크립트**였습니다. 브라우저(React 화면)는 스크립트를 직접 실행할 수 없고, 오직 "웹 요청"만 보낼 수 있어서, 그 요청을 받아서 스크립트 기능을 대신 실행해줄 **API 서버**가 있어야 "화면에서 업로드 버튼 누르기"가 실제 임베딩으로 이어질 수 있습니다. 그래서 `backend/rag/api.py`(FastAPI)를 새로 만들었습니다 — 이게 이 프로젝트에서 처음으로 생긴 "진짜 동작하는" 서버입니다(기존 로그인은 가짜였음).

**무엇을 만들었나:**
- `backend/rag/api.py` — `POST /api/rag/documents`(파일 업로드 → 저장 → 전체 재색인 → 결과 반환), `GET /api/rag/documents`(색인된 문서 목록, 분류·청크 수 포함)
- `src/ragApi.js` — 프론트에서 저 API를 호출하는 함수
- `src/pages/knowledge/KnowledgePage.jsx` — 지식 탭 실제 화면: 파일 선택(.txt/.md만) + 대/중/소분류 입력(선택) + 업로드 버튼 + 색인된 문서 목록(분류 배지, 청크 수)
- `vite.config.js`에 `/api/rag` 프록시 추가 — 프론트(5173번 포트)가 백엔드(8000번 포트)로 요청을 넘겨주는 통로. 이제 개발할 때 **터미널 2개**가 필요합니다: `npm run dev`(화면) + `uvicorn rag.api:app`(RAG 서버)
- `AppLayout.jsx`의 다크 배경 목록(`NO_TOPBAR_PATHS`)에 `/knowledge` 추가 — 요청하신 대로 로그인 화면과 같은 검정 배경 적용

**색상 시스템 적용 방식 (역할 남용 안 하기):**
전달받은 5색(보라/초록/오렌지/노랑/파랑) 중, 지금 화면에 실제로 필요한 역할 2개만 적용했습니다 — **파랑 = 분류 배지(정보성)**, **초록 = "색인 완료" 상태 배지(성공)**. 나머지 3색(보라/오렌지/노랑)은 CSS 변수로는 정의해뒀지만 지금 화면에 억지로 쓰지 않았습니다. 이유는 사용자가 직접 짚어주신 원칙 그대로입니다 — "5개 컬러를 전부 남발하면 위계가 무너진다." 업로드 버튼은 로그인 화면과 동일하게 라임그린(Primary 액션 색)으로 통일했습니다.

**검증한 내용:**
- API 서버를 실제로 띄워서 curl/Python `requests`로 업로드·목록 조회 확인 (분류 포함 색인 정상 동작)
- **버그 발견(테스트 도구 문제로 확인)**: curl로 한글 파일명을 업로드하니 파일명이 깨짐(`Å×½ºÆ®¹®¼­.md` 같은 글자) → 원인 파악해보니 curl 자체의 인코딩 방식 문제였고, 실제 웹 브라우저가 쓰는 방식과 동일한 Python `requests`로 재테스트하니 `기술/AI/AI챗봇/테스트문서.md`로 정확히 저장됨. 실제 코드 버그는 아니었음
- `npm run build` 통과, `KnowledgePage`가 별도 청크로 lazy-loading되는 것 확인
- Vite 프록시 재시작 후 `curl http://127.0.0.1:5173/api/rag/documents`로 실제 5173→8000 전달 확인

**아직 안 된 것:**
- 헤드리스 브라우저(스크린샷) 검증은 이번엔 못 함 — Playwright가 프로젝트 의존성이 아니고 임시 설치도 시간이 오래 걸려서, 대신 API 동작을 커맨드라인으로 충분히 검증하고 실제 화면은 사용자 브라우저로 직접 열어서 확인 부탁드림
- 업로드 시마다 `data/raw/` 전체를 다시 색인(재청킹+ChromaDB 재구성)함 — 문서가 아주 많아지면 느려질 수 있는데, 지금 규모(개인 프로젝트)에서는 문제없음. 임베딩 API 비용은 캐시 덕분에 안 늘어남
- `rag/api.py`는 인증이 전혀 없음 — 로컬 개발 전용, 외부에 노출하면 안 됨

---

### 2026-07-07 (이어서 6) — 문서 업로드 형식을 PDF/DOCX/HWPX까지 확장

**요청 배경:** 지식 탭이 `.txt`/`.md`만 지원했는데, "확장자를 최대한으로 늘려달라"는 요청. 텍스트 형식만 늘릴지, 워드/한글/PDF처럼 실제 문서 파일까지 처리할지 갈리는 지점이라 먼저 확인했고, **PDF/DOCX/HWPX까지 실제 파싱**하는 쪽으로 결정.

**새 개념 설명 — "파싱"이 왜 필요한가:**
`.txt`/`.md`는 파일을 열면 그 자체가 사람이 읽는 글자라서 컴퓨터도 그대로 읽으면 됩니다. 반면 `.pdf`/`.docx`/`.hwpx`는 **글자 모양, 줄 위치, 서식 정보까지 함께 담긴 복잡한 파일 형식**이라, 그 안에서 순수한 텍스트만 뽑아내는 별도 작업(파싱)이 필요합니다. 이번에 `backend/rag/extractors.py`를 새로 만들어 이 역할을 맡겼습니다.

**형식별로 어떻게 텍스트를 뽑았나:**
- **PDF** → `pdfplumber` 라이브러리로 페이지별 텍스트 추출
- **DOCX**(마이크로소프트 워드) → `python-docx` 라이브러리로 문단·표 추출
- **HWPX**(한글과컴퓨터, 요즘 한글 프로그램의 기본 저장 형식) → **라이브러리 없이 직접 파싱**. HWPX 파일은 사실 zip 압축 파일 안에 XML(사람이 읽을 수 있는 태그 구조)이 들어있는 형태라서(워드 docx와 원리가 같음), 압축을 풀어서 XML 안의 문단(`hp:p`)·글자(`hp:t`) 태그만 골라 읽으면 됩니다.

**왜 HWPX는 "한글 프로그램"을 안 쓰기로 했나:** 이 컴퓨터엔 실제 한글 프로그램이 설치돼 있어서, 그 프로그램을 백그라운드로 띄워 파일을 열고 텍스트를 뽑아오는 방법(COM 자동화)도 가능했습니다. 하지만 이 방법은 **서버가 요청을 처리하는 도중에 한글이라는 무거운 외부 프로그램을 매번 띄워야 해서 느리고, 여러 사람이 동시에 요청하면 프로그램이 꼬이거나 멈출 위험**이 있습니다. 반면 zip+XML 직접 읽기는 외부 프로그램 없이 순수 Python 코드만으로 끝나서 서버에서 훨씬 안정적입니다.

**검증한 내용 (실제 파일로):**
- 이 컴퓨터의 한글 프로그램을 딱 한 번, **테스트용 샘플 파일 3개(hwpx/docx/pdf)를 만드는 용도로만** 사용(자동화 스크립트는 테스트 후 바로 삭제) — "이것은 RAG 파일 파싱 테스트를 위한 문단입니다..." 같은 알고 있는 문장을 넣어 저장
- 세 샘플 각각 `extract_text()`로 뽑아보니 원래 넣은 문장이 그대로(문단 구분까지) 나오는 것 확인
- 세 샘플을 실제로 `data/raw/`에 넣고 CLI 파이프라인(`rag.pipeline index`) 전체 실행 → 청크 51개에서 54개로 정확히 3개 늘어남 확인, "AI 챗봇 사업 관련 내용"으로 검색하니 hwpx 샘플이 최상위로 검색됨
- 업로드 API(`POST /api/rag/documents`)로도 실제 PDF 파일을 보내 정상 저장·색인되는지 확인
- 검증 후 테스트 파일·폴더 전부 삭제하고 원래 문서 3개(51청크) 상태로 복원

**남은 미지원 형식:** XLSX(엑셀), 옛날 방식의 바이너리 `.hwp`(HWPX 이전 형식, 별도 파서 필요), 스캔된 이미지로만 이루어진 PDF(글자 인식(OCR)이 필요해서 지금 방식으론 텍스트가 안 뽑힘).

### 2026-07-08 — 보고서 탭 실제 구현 (AI 본문 생성 → 한글 hwpx 다운로드)

**무엇을 만들었나:**
- 사이드바의 "보고서" 탭이 준비중 화면에서 **실제 화면**(`pages/reports/ReportsPage.jsx`)으로 바뀜
- 화면 흐름: 서식 선택 → 제목·간단한 내용 입력(+참고 파일 첨부 가능) → "AI로 본문 생성" → 생성된 내용을 섹션별로 수정 → 미리보기 확인 → "HWPX 다운로드"로 한글 파일 받기
- 백엔드에 `backend/report/` 패키지 신규 추가 (기존 `rag/`와 나란히):
  - `engine.py` — 서식(hwpx) 안의 마커(`{{TITLE}}` 등)를 실제 내용으로 바꾸고 항목·표를 필요한 만큼 복제해 한글 파일을 조립. **한글 프로그램 없이 순수 파이썬으로 hwpx를 만든다**
  - `composer.py` — Claude API로 보고서 본문을 생성. API 키가 없으면 "대체 생성기"가 자리표시 초안을 만들어 흐름은 항상 동작
  - `extractors.py` — 참고 파일(txt/md/csv/**hwp**/hwpx/docx)에서 텍스트 추출. 옛 바이너리 .hwp까지 지원(직접 구현한 OLE 파서)
  - `api.py` — FastAPI 라우터 (`/api/report/*`)
- `backend/main.py` 신규 — RAG API와 보고서 API를 **한 서버로 합쳐서** 띄우는 진입점

**개념 설명 (처음 보는 것들):**
- **hwpx는 사실 ZIP 압축파일**: 확장자만 다를 뿐 안에 XML(문서 설계도)이 들어 있어서, 프로그램 없이 코드로 내용을 바꿔치기할 수 있다. 서식 파일에 `{{TITLE}}` 같은 표시(마커)를 심어두고, 코드가 그 자리를 실제 내용으로 바꾸는 방식.
- **AI는 내용만, 조립은 코드가**: AI(Claude)는 "제목/개요/섹션 항목/표 데이터"를 JSON으로만 만들고, XML 조립은 전부 코드가 한다. AI가 XML을 직접 쓰면 형식이 조금만 틀려도 한글에서 파일이 안 열리기 때문.
- **지침 3계층**: 기본 규칙(코드) < `templates/_공통지침.md`(모든 보고서 공통) < `templates/서식이름.md`(그 서식 전용). 충돌하면 뒤가 이긴다. 예: 공통 지침은 "개조식(~함)"이지만 보도자료 지침은 "서술형(~한다)"이라 보도자료가 이긴다.
- **서식 추가 = 파일 2개**: `templates/서식이름.hwpx`(마커 심은 한글 서식) + `서식이름.md`(작성 지침, 선택). 코드 수정 없이 파일만 넣으면 서식 목록에 자동 등장(서버가 서식 안의 섹션 바를 읽어 목차를 자동 인식).

**왜 이렇게 결정했나:**
- 별도 서버를 하나 더 띄우지 않고 기존 RAG 서버에 라우터만 합침(`main.py`) → 실행 명령이 `uvicorn main:app` 하나로 끝나고, 기존 rag 코드는 한 줄도 안 바꿈
- Claude 호출을 anthropic SDK 대신 표준 라이브러리(urllib)로 구현 → 설치할 패키지가 하나도 안 늘어남(pip이 막힌 기관망에서도 동작)
- 참고 파일 추출기를 rag/extractors.py와 별도로 둠 → rag 쪽은 외부 라이브러리(pdfplumber 등)를 쓰지만 보고서 쪽은 표준 라이브러리만 사용. 서로 의존이 없어 한쪽을 고쳐도 다른 쪽이 안 깨짐

**검증한 내용:**
- (업무 PC, venv 없이 표준 라이브러리만으로) 서식 4종 등록·목차 자동 인식, 지침 3계층 프롬프트 주입, 대체 생성기 → hwpx 조립(잔여 마커 0, XML 문법 통과), 참고 파일 텍스트 추출, 보도자료(섹션 바 없는 서식) 경로까지 전부 통과
- `report/api.py`·`main.py`는 문법 검증만 완료 — **FastAPI 실행 검증은 개발 PC(venv 있는 곳)에서 필요** (아래 "다음에 할 것" 참고)
- 같은 엔진을 쓰는 단독 테스트 앱에서는 실제 Claude API 키로 생성→다운로드→한글에서 열기까지 확인된 상태

**다음에 할 것 / 주의:**
- 개발 PC에서: `cd backend && ./venv/Scripts/python.exe -m uvicorn main:app --reload --port 8000` 로 서버를 띄우고 보고서 탭 전체 흐름 확인 (uvicorn 실행 명령이 `rag.api:app` → `main:app`으로 바뀜!)
- `backend/.env`에 `ANTHROPIC_API_KEY=` 추가하면 실제 AI 생성, 없으면 대체 생성기로 동작
- `검토보고서.hwpx` 서식은 업무 PC의 문서보안(DRM) 때문에 이번 커밋에서 제외됨 — DRM 없는 파일을 구해 `backend/report/templates/`에 넣으면 자동 등록됨(짝이 되는 지침 `검토보고서.md`는 이미 들어 있음)
- 보고서 API도 rag API처럼 인증이 없음 — 로컬 개발 전용, 외부 노출 금지

### 2026-07-08 — 코드 리뷰 지적사항 검증 후 실제 버그 2건 수정 (백엔드 보안)

**무엇을 했나:**
- 코드 리뷰에서 나온 지적 5건을 하나씩 직접 검증하고, 그중 **진짜 버그 2건만** 골라 고침 (나머지 3건은 의도된 설계이거나 이미 처리된 것이라 그대로 둠)

**고친 것 ①: 파일 업로드 경로 조작 막기 (`backend/rag/api.py`)**
- **무엇이 문제였나:** 지식 탭에서 문서를 올릴 때, 파일 이름과 분류명을 **그대로 저장 경로로 사용**하고 있었다. 그래서 파일 이름을 `../../몰래.txt` 처럼 지어 보내면 원래 저장 폴더(`data/raw`) **바깥에도 파일을 쓸 수 있었다**. (이런 걸 "경로 조작(path traversal)" 공격이라고 한다.)
- **어떻게 고쳤나:** 이름에서 폴더 구분자(`/`, `\`)를 잘라 **맨 끝 이름만** 남기는 `_safe_name()` 함수를 추가. `..`·드라이브 문자(`C:`) 같은 위험한 이름은 아예 거절(400 오류). 마지막으로 실제 저장 경로가 정말 `data/raw` 안에 있는지 한 번 더 확인(이중 방어).
- **검증:** `../evil.txt` → `evil.txt`(폴더 탈출 제거), `C:/abs.txt` → `abs.txt`, `..` → 차단됨 — 의도대로 동작 확인.

**고친 것 ②: .env 파일을 실행 위치와 무관하게 읽기 (`rag/api.py`, `report/api.py`)**
- **무엇이 문제였나:** API 키 등을 담는 `backend/.env` 파일을 읽는 `load_dotenv()`가 **명령을 실행한 위치(현재 폴더)를 기준으로** 파일을 찾고 있었다. `cd backend` 하고 실행하면 잘 찾지만, 프로젝트 루트에서 실행하면 못 찾아서 **AI 키가 없는 것처럼 동작**할 수 있었다.
- **어떻게 고쳤나:** 파일 위치를 기준으로 `backend/.env` **절대경로를 딱 고정**해서 읽도록 바꿈. 이제 어느 폴더에서 실행하든 항상 같은 `.env`를 읽는다.
- **검증:** 서버 재시작 후 `/api/report/status`가 `{"ai_ready":true,"key_source":"env"}` 반환 — .env가 제대로 읽히는 것 확인.

**안 고치고 그대로 둔 것 (왜 두었나):**
- **백엔드에 로그인 검증이 없음:** 사실이지만 지금은 **로컬 개인 도구**라 의도된 상태(로그인은 개발용 가짜 인증). 사내망에 배포하는 시점에 인증을 붙이면 됨 — 지금 붙일 필요 없음.
- **AI 호출이 실패하면 전부 "대체 생성기"로 넘어감:** 이건 버그가 아니라 **일부러 그렇게 만든 안전장치**(AI가 안 되도 화면 흐름이 끊기지 않게). 실패 사유는 이미 화면과 서버 로그에 표시됨.
- **ChromaDB의 telemetry 경고 로그:** 끄는 설정(`anonymized_telemetry=False`)이 **이미 들어가 있음**. 남은 경고는 라이브러리(chromadb/posthog) 버전 궁합 문제일 뿐 기능과 무관 — 건드리지 않음.

### 2026-07-08 — 스캔 PDF(487p 회계 교재)를 OCR로 변환해 RAG에 색인

**무엇을 했나:**
- 487페이지짜리 **스캔 이미지 PDF**(지방회계실무 2026 공통교재)를 지식(RAG)에 넣음. 스캔본이라 글자 데이터가 없어서, 그냥 올리면 아무것도 색인되지 않고 CPU만 태우는 문제가 있었음(→ 원본은 `_excluded_docs/`로 격리).
- **OCR**로 이미지→텍스트 변환 후, 그 텍스트(마크다운)를 색인하는 방식으로 해결.

**개념 설명 (처음 보는 것들):**
- **OCR(광학 문자 인식)**: 사진/스캔 속 글자를 컴퓨터가 읽는 텍스트로 바꾸는 기술. 스캔 PDF는 이 단계를 반드시 거쳐야 검색·임베딩이 가능하다.
- **텍스트 PDF vs 스캔 PDF**: 텍스트 PDF는 글자 데이터가 들어 있어 바로 추출되지만, 스캔 PDF는 "사진"이라 OCR 없이는 글자가 0개 나온다. 이번 교재가 후자였다.
- **왜 15페이지씩 잘라서 호출했나**: Upstage OCR의 sync(즉시응답) 방식은 20페이지 미만 권장이라, 큰 문서는 나눠서 여러 번 부른다. 각 배치 결과를 파일로 캐시해서, 중간에 끊겨도 이미 변환한 페이지는 다시 과금하지 않게 했다.

**비용 (실측):**
- OCR: 487페이지 × $0.01 = **$4.87** (Upstage Document Parse)
- 임베딩: 1,046조각, **약 $0.02~0.05** (반올림하면 무시할 수준) — 프로젝트가 이미 쓰는 `embedding-passage`
- **합계 약 $4.9 (≈ 6,600원), 일회성**

**결과:**
- `data/raw/회계/회계실무_2026_공통교재.md` 로 저장 → 폴더 경로 덕분에 **category_l1="회계" 자동 부여**
- **1,046개 조각**으로 색인 완료. 마크다운 표 370개가 그대로 보존됨(회계 교재의 표·숫자 품질 양호).
- 원본 스캔 PDF는 `_excluded_docs/`에 보관(앱이 다시 건드리지 않게).

**왜 폴더로 카테고리를 나눴나:**
- 나중에 '에이전트' 탭에 **회계챗**(회계 문서만 검색)과 'AI챗'(전체 검색)을 만들 계획이라, 지금부터 문서에 `회계` 꼬리표를 달아둠. 임베딩 창고는 하나로 공유하고, 에이전트마다 **검색 필터만** 다르게 거는 구조(메타데이터 필터링). 에이전트를 늘려도 재임베딩 비용은 0원.

**다음에 할 것:**
- 검색/응답 엔드포인트(질문 임베딩→ChromaDB 검색→Claude 근거 기반 답변) + 채팅 UI를 만들어야 회계챗/AI챗이 실제로 동작함(현재 `/agents`·`/chat`은 자리표시자).
- UMAP 임베딩 지도(`umap_coords.json`, 이미 생성됨)를 지식 탭에 "지도 보기"로 붙이면, 회계 군집이 시각적으로 확인됨.

### 2026-07-08 — AI 채팅 / 회계챗 (근거 기반 질의응답) 실제 구현

**무엇을 만들었나:**
- 그동안 자리표시자였던 **'AI 채팅'** 탭과 **'에이전트'** 탭을 실제로 동작하게 만듦.
- 이제 지식 탭에 올린 문서를 **근거로 삼아 질문에 답하는 채팅**이 된다. 답변에는 **[1][2] 근거 번호**가 붙고, 어느 문서 어느 조각에서 나왔는지 펼쳐볼 수 있다.
- 두 가지 모드가 **같은 화면 하나**로 동작:
  - **AI 채팅**(`/chat`) — 전체 문서에서 검색
  - **회계챗**(에이전트 탭 → 카드 클릭 → `/chat?scope=회계`) — 회계 분야 문서만 검색

**개념 설명 (처음 보는 것들):**
- **RAG 질의응답의 흐름**: 질문을 임베딩 → ChromaDB에서 가장 비슷한 조각들을 찾아옴 → 그 조각들을 Claude에게 "이 근거만 써서 답하라"고 넘김 → 근거 기반 답변. AI가 아는 척 지어내지 않고 **올린 문서 안에서만** 답하게 하는 게 핵심.
- **에이전트 = 검색 필터 + 성격**: '회계챗'은 별도의 AI가 아니다. **같은 임베딩 창고**를 쓰되 `category_l1="회계"` 필터만 걸어 회계 문서만 뒤지는 것. 그래서 에이전트를 몇 개 만들든 **재임베딩 비용이 0원**이고, 문서를 그 분류로 올려두기만 하면 새 에이전트가 바로 동작한다.
- **근거 표시(citation)**: 답변 아래 "근거 N개"를 펼치면 실제 출처 조각을 볼 수 있어, AI 답이 맞는지 사람이 검증할 수 있다.

**어떻게 만들었나 (구조):**
- 백엔드 `backend/rag/chat.py` 신규 — 질문 임베딩→검색(카테고리 필터)→Claude 답변 생성. 검색은 이미 있던 `store.query()`(필터 지원)를 그대로 재사용. Claude 호출은 `report/composer.py`와 같은 표준 라이브러리(urllib) 방식(설치 패키지 안 늘림).
- `backend/rag/api.py`에 `POST /api/rag/chat` 엔드포인트 추가. body의 `category_l1`이 있으면 그 분야만, 없으면 전체 검색.
- 답변 모델은 대화형이라 빠른 `claude-sonnet-5` 사용. API 키가 없거나 실패하면 **찾은 근거 조각만 보여주는 fallback**(보고서 탭과 같은 안전장치).
- 프론트: `ragApi.js`에 `chat()` 추가, `pages/chat/ChatPage.jsx`(질문·답변·근거 UI, URL 파라미터로 회계챗/AI챗 겸용), `pages/agents/AgentsPage.jsx`(에이전트 허브 카드) + `agentsConfig.js`(에이전트 목록 = 데이터). `App.jsx`에 라우트 연결, `/chat`을 다크 배경 목록에 추가.

**검증한 내용:**
- 백엔드 직접 호출: 회계 질문("수의계약이 가능한 경우?")→ 회계 교재에서만 근거 추출, 근거번호 포함 답변. 프로젝트 질문("기술 스택?")→ 필터 없이 agent.md/devlog에서 근거 추출. **필터 동작 확인**.
- `npm run build` 통과(ChatPage/AgentsPage 청크 정상 생성).
- 브라우저와 동일한 fetch(한글 본문, Vite 프록시 경유)로 HTTP 200 + 근거 기반 답변 확인.
- (참고) Windows에서 curl로 한글 JSON을 프록시에 보내면 "error parsing the body"가 나는데, 이는 **curl 특유의 인코딩 문제**이고 브라우저 fetch에서는 정상. 실제 앱 경로엔 영향 없음.

**다음에 할 것:**
- UMAP 임베딩 지도를 지식 탭에 "지도 보기"로 붙이기(다음 작업). 이제 회계 군집이 색인돼 있어 지도에 크게 보일 것.
- 채팅 답변의 마크다운을 실제 서식으로 렌더링(지금은 줄바꿈만 보존). 대화 기록 저장(localStorage)도 추후.

### 2026-07-08 — 임베딩 지도(UMAP)를 지식 탭에 붙임

**무엇을 만들었나:**
- 지식 탭에 **[목록] / [지도]** 토글을 추가. '지도'를 누르면 올린 문서들이 **점으로 흩뿌려진 그림**으로 보인다. 비슷한 내용의 조각끼리 가까이 뭉쳐서, 지식 창고의 "모양"을 한눈에 볼 수 있다.
- 점 색깔 = 출처 문서. 범례를 클릭해 특정 문서만 켜고 끌 수 있고, 점에 마우스를 올리면 그 조각의 미리보기가 뜬다.

**개념 설명 (처음 보는 것들):**
- **임베딩은 4096차원**이라 사람이 볼 수 없다. **UMAP**은 이 고차원 점들을 "가까운 건 가깝게" 유지하며 **2차원으로 눌러 펴서** 그림으로 그릴 수 있게 해주는 기술이다.
- **주의**: UMAP은 "누가 누구와 이웃인지(뭉침)"는 잘 보존하지만, **군집 사이의 거리·크기는 왜곡**한다. 그래서 "같은 주제끼리 뭉치는가"를 보는 진단용이지, 2D 거리로 유사도를 재는 도구가 아니다.
- **왜 지식 탭 안에 넣었나**: 지도는 결국 "지식 창고를 눈으로 본 것"이라, 문서 목록과 같은 곳에 두는 게 자연스럽다(목록↔지도 토글).

**어떻게 만들었나:**
- 백엔드 `backend/rag/api.py`에 두 엔드포인트 추가:
  - `GET /api/rag/umap` — 저장된 좌표를 돌려줌. 색인 조각 수와 좌표 수가 다르면 `stale=True`(지도가 낡음)로 알려줌.
  - `POST /api/rag/umap` — 현재 ChromaDB의 **저장된 임베딩을 그대로 꺼내** UMAP 2D 좌표를 다시 계산해 저장. **임베딩 API 비용 0원**(이미 만든 벡터 재사용). umap-learn 첫 계산은 수십 초 걸려서, 업로드마다 자동으로 하지 않고 "지도 만들기" 버튼을 눌렀을 때만 계산한다.
- 프론트: `pages/knowledge/EmbeddingMap.jsx`(순수 SVG 산점도 — 외부 차트 라이브러리 없이 점·범례·툴팁 직접 구현), `ragApi.js`에 `getUmap()`/`rebuildUmap()` 추가, `KnowledgePage.jsx`에 목록/지도 토글과 "새로 만들기" 배너.

**검증한 내용:**
- `GET /api/rag/umap`: 옛 좌표 51개 vs 현재 1097개 → `stale=True` 정확히 감지.
- `POST /api/rag/umap`: 71초 소요, 1097개 좌표 생성. 출처별 = 회계 교재 1046 + 프로젝트 문서 51. **회계 군집이 지도에 포함됨** 확인.
- `npm run build` 통과, 브라우저와 동일한 fetch(프록시 경유)로 `points: 1097, stale: false` 확인.

**다음에 할 것 / 한계:**
- 업로드 후 "지도가 낡았어요" 배너가 뜨면 사용자가 "새로 만들기"를 눌러야 갱신됨(자동 아님 — UMAP이 무겁기 때문). 나중에 백그라운드 재계산으로 개선 가능.
- 색상은 출처 파일 기준. `category_l1`(예: 회계) 기준 색칠 옵션을 추가하면 에이전트별 영역이 더 뚜렷해짐.

### 2026-07-10 — 배포 준비 1·2단계: 백엔드 주소 스위치 + CORS 명단 환경변수화

**왜 하나 (개념):**
- 이 앱을 인터넷에 올리려면 **화면(프론트)**과 **두뇌(백엔드)**를 각각 다른 서비스에 올려야 한다. 계획: 화면=Vercel(무료), 백엔드=Render(파이썬 서버, 영구 디스크 필요). 임베딩·ChromaDB는 Vercel에서 못 돌리기 때문(정적 호스팅).
- 임베딩 데이터 방침: 원본 문서(`data/raw`, 예: 회계 교재를 텍스트로 변환한 988KB .md)는 이미 git에 있으므로, **무거운 벡터DB(`data/chroma`)나 원본 PDF(242MB)는 옮기지 않고** 서버에서 `python -m rag.pipeline index`로 재색인해 다시 만든다(원본이 작아 비용 미미). 문서를 새로 넣을 때만 재색인, 코드 배포와는 분리.

**무엇을 했나:**
- **1단계 — 백엔드 주소 스위치**: 프론트가 백엔드를 부를 때 지금은 `/api/...`만 적혀 있어 개발 중엔 Vite 프록시가 localhost:8000으로 연결해준다. 배포되면 그 프록시가 없어 깨진다. 그래서 `src/apiBase.js`(신규)에 `API_BASE = import.meta.env.VITE_API_BASE || ''`를 두고, 백엔드를 부르는 4개 파일(`api.js`·`ragApi.js`·`reportApi.js`·`hooks/useServerState.js`)이 모두 이 값을 앞에 붙이게 했다. 개발에선 빈 값이라 **기존과 동작 동일**, 배포 빌드 때 `VITE_API_BASE`에 Render 주소를 넣으면 그쪽을 본다.
- **2단계 — CORS 친구 명단 환경변수화**: 브라우저는 "다른 출처(Vercel)에서 온 화면이 다른 출처(Render) 서버에 요청"하는 걸 기본 차단한다(CORS). 백엔드가 Vercel 주소를 허용 명단에 넣어야 한다. 코드에 주소를 박는 대신 `rag/config.py`에 `allowed_origins()`를 추가 — 기본은 localhost:5173, 환경변수 `FRONTEND_ORIGINS`(콤마 구분)에 넣은 주소를 더한다. `rag/api.py`와 `main.py`(RAG 미탑재 fallback 경로) 두 곳의 CORS가 이 함수를 쓰도록 통일하고, 나중 로그인(쿠키) 대비 `allow_credentials=True`도 켰다.

**검증한 내용:**
- 프론트: `npm run build` 성공 + `npm test` 11개 전부 통과(기존 기능 안 깨짐). `VITE_API_BASE`가 비어 있어 내 PC 동작은 이전과 동일.
- 백엔드: `allowed_origins()`가 기본값/환경변수 주입값 모두 정확히 반환. `import main`으로 앱 로드 정상(라우트 15개, RAG 포함 기동).

**이어서 3·4단계도 완료(같은 날):**
- **3단계 — 영구 디스크 대비**: Render는 재배포 시 서버 파일이 초기화되므로 벡터DB가 날아간다. `rag/config.py`의 생성물 경로(chunks·cache·chroma·umap)를 새 `DATA_DIR`(=환경변수 `RSG_DATA_DIR` 또는 기본 `repo/data`) 기준으로 바꿔, 배포 시 영구 디스크(`/var/data`)를 가리키게 함. 원본 `RAW_DOCS_DIR`은 git 원천이라 그대로 repo 안을 본다. 검증: 환경변수 없으면 기존 경로(`data/chroma`) 동일, `RSG_DATA_DIR` 주면 디스크 경로로 전환됨 확인.
- `render.yaml`(신규, 저장소 루트): Render Blueprint. rootDir=backend, `pip install -r requirements.txt` → `uvicorn main:app`, 비밀키(`ANTHROPIC_API_KEY`·`UPSTAGE_API_KEY`·`FRONTEND_ORIGINS`)는 대시보드 입력(sync:false), `RSG_DATA_DIR=/var/data` + 동일 경로 영구 디스크(1GB). 영구 디스크는 무료 플랜 불가라 plan=starter.
- `vercel.json`(신규, 루트): react-router 주소 새로고침 시 404 방지용 SPA fallback(`/(.*) → /index.html`).
- **4단계 — `docs/DEPLOY.md`(신규)**: Render 백엔드 + Vercel 프론트 배포 절차, 환경변수 표, 첫 재색인(`python -m rag.pipeline index`, Render Shell), 이후 개발 흐름(git push=자동배포/문서추가 시만 재색인), 그리고 **로그인 미구현 경고**(배포 전 Vercel 비밀번호 보호 등으로 임시 방어)까지 초보자용으로 정리.
- 검증: `npm run build` 성공, 백엔드 `import main` 정상(라우트 15), `vercel.json`/`render.yaml` 문법 유효.

**다음에 할 것 (5·6단계):**
- 5단계: **로그인/잠금** — 현재 로그인은 dev 전용 가짜(`vite.config.js`의 `mockAuth`)라 배포 시 사라진다. 아무나 접속해 Claude 비용을 쓰지 못하게 최소한의 문을 달아야 함(방식 미정 — 사용자 결정 필요).
- 6단계: GitHub↔Render/Vercel 연결로 실제 배포(=이후 `git push`가 곧 자동배포).

### 2026-07-10 (이어서) — 5단계 로그인: 백엔드 절반(문지기) 구현

**결정:** 사용자가 "사람별 계정(아이디+비번)" 선택. 화면(Vercel)↔백엔드(Render)가 다른 출처라 쿠키 대신 **토큰-헤더 방식**(로그인 시 토큰 발급 → 요청마다 `Authorization: Bearer` 로 지참)을 채택 — local·배포에서 동일하게 동작해 초보자가 다루기 쉽고 CORS 문제도 적음. 공개 회원가입은 두지 않고 **CLI로 계정 생성**(소수 팀 보안).

**무엇을 만들었나 (백엔드, 신규 `backend/auth/` 패키지):**
- `auth/store.py` — 회원 DB(users)+세션(sessions) SQLite. 비밀번호는 pbkdf2(표준 라이브러리, 무작위 salt, 20만 회)로 해시 저장, 상수시간 비교. DB는 `rag.config.DATA_DIR/app.db`(영구 디스크 대응). 함수: create/list/delete/set_password/authenticate/create_session/user_for_token/destroy_session. **새 패키지 의존성 0**(전부 표준 라이브러리).
- `auth/api.py` — 라우터(`POST /api/auth/login` {loginId,password}→{user,token}, `POST /api/auth/logout`, `GET /api/auth/me`) + `require_user` 의존성 + **`AuthMiddleware` 문지기**: `/api/rag`·`/api/report`·`/api/store` 요청에 유효 토큰이 없으면 401. `/api/auth/*`와 OPTIONS(프리플라이트)는 통과. 401 응답에도 CORS 헤더를 직접 붙여 브라우저가 내용을 읽게 함.
- `auth/manage.py` — 계정 관리 CLI: `python -m auth.manage add/list/passwd/delete`. 배포 후 Render Shell에서 팀원 계정 생성.
- `main.py` — auth 라우터 등록 + `AuthMiddleware` 추가.

**검증(임시 DATA_DIR에서 실서버 기동, 실데이터 미접촉):**
- CLI로 계정 생성/목록 OK. `authenticate`: 맞는 비번 True, 틀린 비번 False.
- HTTP 4종: ① 토큰 없이 `/api/store/todos`→401 차단, ② 로그인→토큰 발급, ③ 토큰 지참→200 통과, ④ 틀린 비번 로그인→401. 전부 기대대로.

### 2026-07-10 (이어서) — 5단계 로그인: 프론트 절반(화면 연결) 완료 → 로그인 시스템 완성

**무엇을 했나 (프론트):**
- `src/apiBase.js`에 출입증(토큰) 보관소 추가: `getToken/setToken/clearToken`(localStorage `rsg_token`) + `authHeaders()`(요청에 붙일 `Authorization: Bearer`). Content-Type은 안 넣어 FormData 업로드를 방해하지 않음.
- `src/api.js`: `apiLogin`이 백엔드의 `{user, token}`에서 토큰을 보관하고 사용자만 반환. `apiLogout`은 서버 폐기 후 토큰 삭제. `req`가 모든 요청에 `authHeaders()`를 붙이고, 401이면 토큰을 비워 다음 세션 확인 때 로그인 화면으로 유도.
- 백엔드를 부르는 나머지 3파일(`ragApi.js`·`reportApi.js`·`hooks/useServerState.js`)의 모든 fetch에 `authHeaders()` 병합(GET·PUT·업로드·다운로드 포함). 백엔드가 토큰을 요구하므로 이걸 해야 지식·보고서·워크스페이스가 다시 동작.
- `vite.config.js`: 가짜 로그인 `mockAuth` 플러그인 **완전 제거**, `server.proxy`에 `/api/auth` 추가(dev에서도 진짜 백엔드 로그인 사용). vitest 설정은 그대로.

**검증:**
- `npm run build` 성공, `npm test` 11개 전부 통과(테스트는 api.js를 모킹하므로 시그니처 동일해 영향 없음).
- 실서버(포트 8022, 진짜 `data/app.db`) + CLI로 만든 `admin` 계정으로 전 과정 확인: ① 로그인→토큰+사용자('관리자'), ② 토큰 없이 `/api/store/todos`→401, ③ 토큰 지참→200, ④ `/api/auth/me`→정확한 사용자. 백엔드 `import main` 로그에 "로그인 보호 켜짐" 확인.
- `docs/DEPLOY.md`의 경고를 "로그인 구현됨 + 배포 후 `auth.manage add`로 계정 생성"으로 갱신.

**개발 워크플로 변경(중요):** 이제 dev에서도 **백엔드를 띄워야** 로그인된다(가짜 로그인 없어짐). 로컬 계정은 `cd backend && python -m auth.manage add <아이디> <비번>`으로 만든다. 이번에 `admin`/`admin1234`(표시이름 관리자)를 로컬 `data/app.db`에 생성해둠 — **비밀번호는 바꾸는 걸 권장**.

**다음에 할 것 (6단계):**
- GitHub↔Render/Vercel 연결로 실제 배포. 절차는 `docs/DEPLOY.md` 참고. 이후 `git push`가 곧 자동배포.
