node_modules
dist
.DS_Store
*.local

# RAG 파이프라인 (backend/rag) — PRD-RAG.md 참고
backend/venv
.env
# data/ 하위는 대부분 무시하되, 원본 문서(data/raw)만 git으로 추적한다.
# (다른 PC에서 `python -m rag.pipeline index`로 재색인하면 chroma/cache/umap은 재생성됨)
data/*
!data/raw/
output/
__pycache__/
*.pyc

# 파이프라인 밖으로 격리한 원본 문서·OCR 임시 산출물 (대용량, 로컬 전용)
_excluded_docs/
